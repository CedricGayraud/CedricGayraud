name: Generate Snake Car

on:
  schedule:
    - cron: "0 0 * * *" # chaque jour à minuit
  workflow_dispatch: # exécution manuelle
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_USER_NAME: CedricGayraud

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Clone snake-car engine
        run: |
          git clone https://github.com/CedricGayraud/snk-car.git
          cd snk-car

          echo "📦 Installation des dépendances..."
          npm install
          npm install ts-node typescript --save-dev

          echo "🏎️ Génération du Snake Car SVG..."
          mkdir -p ../dist
          export GITHUB_USER_NAME=CedricGayraud
          export GH_TOKEN=${{ secrets.GH_TOKEN }}

          npx ts-node packages/action/index.ts \
            --user CedricGayraud \
            --output ../dist/github-contribution-grid-snake.svg

          echo "✅ Génération terminée"

      - name: Push snake car animation to the output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
